# Server-only base URL for the ArahIn Go backend.
# Browser requests still go to /api/* on the Next.js app; route handlers proxy
# them to this service so auth tokens can stay in httpOnly cookies.
ARAHIN_API_URL=https://arahin-backend-hxf3nwliva-as.a.run.app
